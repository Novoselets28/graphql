import React from 'react';
import ReactDOM from 'react-dom';
import ApolloProviderWrapper from './ApolloProvider';

const root = document.getElementById('root');
ReactDOM.createRoot(root).render(<ApolloProviderWrapper />);
